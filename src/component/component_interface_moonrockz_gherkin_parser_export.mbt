// Generated by `wit-bindgen` 0.52.0. DO NOT EDIT!

///|
pub fn wasmExportParse(p0 : Int, p1 : Int, p2 : Int, p3 : Int, p4 : Int) -> Int {
  let lifted : String? = match p0 {
    0 => Option::None
    1 => {
      let result = mbt_ffi_ptr2str(p1, p2)
      Option::Some(result)
    }
    _ => panic()
  }
  let result0 = mbt_ffi_ptr2str(p3, p4)
  let result1 : Result[@types.GherkinDocument, Array[@types.ParseError]] = @parser.parse(@types.Source::{
      uri: lifted,
      data: result0,
    },
  )
  match result1 {
    Ok(payload) => {
      mbt_ffi_store8(return_area + 0, 0)
      match payload.source.uri {
        None => {
          mbt_ffi_store8(return_area + 4, 0)
          ()
        }
        Some(payload3) => {
          mbt_ffi_store8(return_area + 4, 1)
          let ptr = mbt_ffi_str2ptr(payload3)
          mbt_ffi_store32(return_area + 12, payload3.length())
          mbt_ffi_store32(return_area + 8, ptr)
          ()
        }
      }
      let ptr4 = mbt_ffi_str2ptr(payload.source.data)
      mbt_ffi_store32(return_area + 20, payload.source.data.length())
      mbt_ffi_store32(return_area + 16, ptr4)
      match payload.feature {
        None => {
          mbt_ffi_store8(return_area + 24, 0)
          ()
        }
        Some(payload6) => {
          mbt_ffi_store8(return_area + 24, 1)
          mbt_ffi_store32(return_area + 28, payload6.location.line)
          match payload6.location.column {
            None => {
              mbt_ffi_store8(return_area + 32, 0)
              ()
            }
            Some(payload8) => {
              mbt_ffi_store8(return_area + 32, 1)
              mbt_ffi_store32(return_area + 36, payload8)
              ()
            }
          }
          let address = mbt_ffi_malloc(payload6.tags.length() * 28)
          for index = 0; index < payload6.tags.length(); index = index + 1 {
            let iter_elem : @types.Tag = payload6.tags[index]
            let iter_base = address + index * 28
            mbt_ffi_store32(iter_base + 0, iter_elem.location.line)
            match iter_elem.location.column {
              None => {
                mbt_ffi_store8(iter_base + 4, 0)
                ()
              }
              Some(payload10) => {
                mbt_ffi_store8(iter_base + 4, 1)
                mbt_ffi_store32(iter_base + 8, payload10)
                ()
              }
            }
            let ptr11 = mbt_ffi_str2ptr(iter_elem.name)
            mbt_ffi_store32(iter_base + 16, iter_elem.name.length())
            mbt_ffi_store32(iter_base + 12, ptr11)
            let ptr12 = mbt_ffi_str2ptr(iter_elem.id)
            mbt_ffi_store32(iter_base + 24, iter_elem.id.length())
            mbt_ffi_store32(iter_base + 20, ptr12)
          }
          mbt_ffi_store32(return_area + 44, payload6.tags.length())
          mbt_ffi_store32(return_area + 40, address)
          let ptr13 = mbt_ffi_str2ptr(payload6.language)
          mbt_ffi_store32(return_area + 52, payload6.language.length())
          mbt_ffi_store32(return_area + 48, ptr13)
          let ptr14 = mbt_ffi_str2ptr(payload6.keyword)
          mbt_ffi_store32(return_area + 60, payload6.keyword.length())
          mbt_ffi_store32(return_area + 56, ptr14)
          let ptr15 = mbt_ffi_str2ptr(payload6.name)
          mbt_ffi_store32(return_area + 68, payload6.name.length())
          mbt_ffi_store32(return_area + 64, ptr15)
          let ptr16 = mbt_ffi_str2ptr(payload6.description)
          mbt_ffi_store32(return_area + 76, payload6.description.length())
          mbt_ffi_store32(return_area + 72, ptr16)
          let address260 = mbt_ffi_malloc(payload6.children.length() * 76)
          for index261 = 0
              index261 < payload6.children.length()
              index261 = index261 + 1 {
            let iter_elem : @types.FeatureChild = payload6.children[index261]
            let iter_base = address260 + index261 * 76
            match iter_elem {
              Background(payload17) => {
                mbt_ffi_store8(iter_base + 0, 0)
                mbt_ffi_store32(iter_base + 4, payload17.location.line)
                match payload17.location.column {
                  None => {
                    mbt_ffi_store8(iter_base + 8, 0)
                    ()
                  }
                  Some(payload19) => {
                    mbt_ffi_store8(iter_base + 8, 1)
                    mbt_ffi_store32(iter_base + 12, payload19)
                    ()
                  }
                }
                let ptr20 = mbt_ffi_str2ptr(payload17.keyword)
                mbt_ffi_store32(iter_base + 20, payload17.keyword.length())
                mbt_ffi_store32(iter_base + 16, ptr20)
                let ptr21 = mbt_ffi_str2ptr(payload17.name)
                mbt_ffi_store32(iter_base + 28, payload17.name.length())
                mbt_ffi_store32(iter_base + 24, ptr21)
                let ptr22 = mbt_ffi_str2ptr(payload17.description)
                mbt_ffi_store32(iter_base + 36, payload17.description.length())
                mbt_ffi_store32(iter_base + 32, ptr22)
                let ptr23 = mbt_ffi_str2ptr(payload17.id)
                mbt_ffi_store32(iter_base + 44, payload17.id.length())
                mbt_ffi_store32(iter_base + 40, ptr23)
                let address52 = mbt_ffi_malloc(payload17.steps.length() * 88)
                for index53 = 0
                    index53 < payload17.steps.length()
                    index53 = index53 + 1 {
                  let iter_elem : @types.Step = payload17.steps[index53]
                  let iter_base = address52 + index53 * 88
                  mbt_ffi_store32(iter_base + 0, iter_elem.location.line)
                  match iter_elem.location.column {
                    None => {
                      mbt_ffi_store8(iter_base + 4, 0)
                      ()
                    }
                    Some(payload25) => {
                      mbt_ffi_store8(iter_base + 4, 1)
                      mbt_ffi_store32(iter_base + 8, payload25)
                      ()
                    }
                  }
                  let ptr26 = mbt_ffi_str2ptr(iter_elem.keyword)
                  mbt_ffi_store32(iter_base + 16, iter_elem.keyword.length())
                  mbt_ffi_store32(iter_base + 12, ptr26)
                  mbt_ffi_store8(
                    iter_base + 20,
                    iter_elem.keyword_type.ordinal(),
                  )
                  let ptr27 = mbt_ffi_str2ptr(iter_elem.text)
                  mbt_ffi_store32(iter_base + 28, iter_elem.text.length())
                  mbt_ffi_store32(iter_base + 24, ptr27)
                  let ptr28 = mbt_ffi_str2ptr(iter_elem.id)
                  mbt_ffi_store32(iter_base + 36, iter_elem.id.length())
                  mbt_ffi_store32(iter_base + 32, ptr28)
                  match iter_elem.argument {
                    None => {
                      mbt_ffi_store8(iter_base + 40, 0)
                      ()
                    }
                    Some(payload30) => {
                      mbt_ffi_store8(iter_base + 40, 1)
                      match payload30 {
                        DocString(payload31) => {
                          mbt_ffi_store8(iter_base + 44, 0)
                          mbt_ffi_store32(
                            iter_base + 48,
                            payload31.location.line,
                          )
                          match payload31.location.column {
                            None => {
                              mbt_ffi_store8(iter_base + 52, 0)
                              ()
                            }
                            Some(payload33) => {
                              mbt_ffi_store8(iter_base + 52, 1)
                              mbt_ffi_store32(iter_base + 56, payload33)
                              ()
                            }
                          }
                          match payload31.media_type {
                            None => {
                              mbt_ffi_store8(iter_base + 60, 0)
                              ()
                            }
                            Some(payload35) => {
                              mbt_ffi_store8(iter_base + 60, 1)
                              let ptr36 = mbt_ffi_str2ptr(payload35)
                              mbt_ffi_store32(
                                iter_base + 68,
                                payload35.length(),
                              )
                              mbt_ffi_store32(iter_base + 64, ptr36)
                              ()
                            }
                          }
                          let ptr37 = mbt_ffi_str2ptr(payload31.content)
                          mbt_ffi_store32(
                            iter_base + 76,
                            payload31.content.length(),
                          )
                          mbt_ffi_store32(iter_base + 72, ptr37)
                          let ptr38 = mbt_ffi_str2ptr(payload31.delimiter)
                          mbt_ffi_store32(
                            iter_base + 84,
                            payload31.delimiter.length(),
                          )
                          mbt_ffi_store32(iter_base + 80, ptr38)
                          ()
                        }
                        DataTable(payload39) => {
                          mbt_ffi_store8(iter_base + 44, 1)
                          mbt_ffi_store32(
                            iter_base + 48,
                            payload39.location.line,
                          )
                          match payload39.location.column {
                            None => {
                              mbt_ffi_store8(iter_base + 52, 0)
                              ()
                            }
                            Some(payload41) => {
                              mbt_ffi_store8(iter_base + 52, 1)
                              mbt_ffi_store32(iter_base + 56, payload41)
                              ()
                            }
                          }
                          let address50 = mbt_ffi_malloc(
                            payload39.rows.length() * 28,
                          )
                          for index51 = 0
                              index51 < payload39.rows.length()
                              index51 = index51 + 1 {
                            let iter_elem : @types.TableRow = payload39.rows[index51]
                            let iter_base = address50 + index51 * 28
                            mbt_ffi_store32(
                              iter_base + 0,
                              iter_elem.location.line,
                            )
                            match iter_elem.location.column {
                              None => {
                                mbt_ffi_store8(iter_base + 4, 0)
                                ()
                              }
                              Some(payload43) => {
                                mbt_ffi_store8(iter_base + 4, 1)
                                mbt_ffi_store32(iter_base + 8, payload43)
                                ()
                              }
                            }
                            let ptr44 = mbt_ffi_str2ptr(iter_elem.id)
                            mbt_ffi_store32(
                              iter_base + 16,
                              iter_elem.id.length(),
                            )
                            mbt_ffi_store32(iter_base + 12, ptr44)
                            let address48 = mbt_ffi_malloc(
                              iter_elem.cells.length() * 20,
                            )
                            for index49 = 0
                                index49 < iter_elem.cells.length()
                                index49 = index49 + 1 {
                              let iter_elem : @types.TableCell = iter_elem.cells[index49]
                              let iter_base = address48 + index49 * 20
                              mbt_ffi_store32(
                                iter_base + 0,
                                iter_elem.location.line,
                              )
                              match iter_elem.location.column {
                                None => {
                                  mbt_ffi_store8(iter_base + 4, 0)
                                  ()
                                }
                                Some(payload46) => {
                                  mbt_ffi_store8(iter_base + 4, 1)
                                  mbt_ffi_store32(iter_base + 8, payload46)
                                  ()
                                }
                              }
                              let ptr47 = mbt_ffi_str2ptr(iter_elem.value)
                              mbt_ffi_store32(
                                iter_base + 16,
                                iter_elem.value.length(),
                              )
                              mbt_ffi_store32(iter_base + 12, ptr47)
                            }
                            mbt_ffi_store32(
                              iter_base + 24,
                              iter_elem.cells.length(),
                            )
                            mbt_ffi_store32(iter_base + 20, address48)
                          }
                          mbt_ffi_store32(
                            iter_base + 64,
                            payload39.rows.length(),
                          )
                          mbt_ffi_store32(iter_base + 60, address50)
                          ()
                        }
                      }
                      ()
                    }
                  }
                }
                mbt_ffi_store32(iter_base + 52, payload17.steps.length())
                mbt_ffi_store32(iter_base + 48, address52)
                ()
              }
              Scenario(payload54) => {
                mbt_ffi_store8(iter_base + 0, 1)
                mbt_ffi_store32(iter_base + 4, payload54.location.line)
                match payload54.location.column {
                  None => {
                    mbt_ffi_store8(iter_base + 8, 0)
                    ()
                  }
                  Some(payload56) => {
                    mbt_ffi_store8(iter_base + 8, 1)
                    mbt_ffi_store32(iter_base + 12, payload56)
                    ()
                  }
                }
                let address61 = mbt_ffi_malloc(payload54.tags.length() * 28)
                for index62 = 0
                    index62 < payload54.tags.length()
                    index62 = index62 + 1 {
                  let iter_elem : @types.Tag = payload54.tags[index62]
                  let iter_base = address61 + index62 * 28
                  mbt_ffi_store32(iter_base + 0, iter_elem.location.line)
                  match iter_elem.location.column {
                    None => {
                      mbt_ffi_store8(iter_base + 4, 0)
                      ()
                    }
                    Some(payload58) => {
                      mbt_ffi_store8(iter_base + 4, 1)
                      mbt_ffi_store32(iter_base + 8, payload58)
                      ()
                    }
                  }
                  let ptr59 = mbt_ffi_str2ptr(iter_elem.name)
                  mbt_ffi_store32(iter_base + 16, iter_elem.name.length())
                  mbt_ffi_store32(iter_base + 12, ptr59)
                  let ptr60 = mbt_ffi_str2ptr(iter_elem.id)
                  mbt_ffi_store32(iter_base + 24, iter_elem.id.length())
                  mbt_ffi_store32(iter_base + 20, ptr60)
                }
                mbt_ffi_store32(iter_base + 20, payload54.tags.length())
                mbt_ffi_store32(iter_base + 16, address61)
                mbt_ffi_store8(iter_base + 24, payload54.kind.ordinal())
                let ptr63 = mbt_ffi_str2ptr(payload54.keyword)
                mbt_ffi_store32(iter_base + 32, payload54.keyword.length())
                mbt_ffi_store32(iter_base + 28, ptr63)
                let ptr64 = mbt_ffi_str2ptr(payload54.name)
                mbt_ffi_store32(iter_base + 40, payload54.name.length())
                mbt_ffi_store32(iter_base + 36, ptr64)
                let ptr65 = mbt_ffi_str2ptr(payload54.description)
                mbt_ffi_store32(iter_base + 48, payload54.description.length())
                mbt_ffi_store32(iter_base + 44, ptr65)
                let ptr66 = mbt_ffi_str2ptr(payload54.id)
                mbt_ffi_store32(iter_base + 56, payload54.id.length())
                mbt_ffi_store32(iter_base + 52, ptr66)
                let address95 = mbt_ffi_malloc(payload54.steps.length() * 88)
                for index96 = 0
                    index96 < payload54.steps.length()
                    index96 = index96 + 1 {
                  let iter_elem : @types.Step = payload54.steps[index96]
                  let iter_base = address95 + index96 * 88
                  mbt_ffi_store32(iter_base + 0, iter_elem.location.line)
                  match iter_elem.location.column {
                    None => {
                      mbt_ffi_store8(iter_base + 4, 0)
                      ()
                    }
                    Some(payload68) => {
                      mbt_ffi_store8(iter_base + 4, 1)
                      mbt_ffi_store32(iter_base + 8, payload68)
                      ()
                    }
                  }
                  let ptr69 = mbt_ffi_str2ptr(iter_elem.keyword)
                  mbt_ffi_store32(iter_base + 16, iter_elem.keyword.length())
                  mbt_ffi_store32(iter_base + 12, ptr69)
                  mbt_ffi_store8(
                    iter_base + 20,
                    iter_elem.keyword_type.ordinal(),
                  )
                  let ptr70 = mbt_ffi_str2ptr(iter_elem.text)
                  mbt_ffi_store32(iter_base + 28, iter_elem.text.length())
                  mbt_ffi_store32(iter_base + 24, ptr70)
                  let ptr71 = mbt_ffi_str2ptr(iter_elem.id)
                  mbt_ffi_store32(iter_base + 36, iter_elem.id.length())
                  mbt_ffi_store32(iter_base + 32, ptr71)
                  match iter_elem.argument {
                    None => {
                      mbt_ffi_store8(iter_base + 40, 0)
                      ()
                    }
                    Some(payload73) => {
                      mbt_ffi_store8(iter_base + 40, 1)
                      match payload73 {
                        DocString(payload74) => {
                          mbt_ffi_store8(iter_base + 44, 0)
                          mbt_ffi_store32(
                            iter_base + 48,
                            payload74.location.line,
                          )
                          match payload74.location.column {
                            None => {
                              mbt_ffi_store8(iter_base + 52, 0)
                              ()
                            }
                            Some(payload76) => {
                              mbt_ffi_store8(iter_base + 52, 1)
                              mbt_ffi_store32(iter_base + 56, payload76)
                              ()
                            }
                          }
                          match payload74.media_type {
                            None => {
                              mbt_ffi_store8(iter_base + 60, 0)
                              ()
                            }
                            Some(payload78) => {
                              mbt_ffi_store8(iter_base + 60, 1)
                              let ptr79 = mbt_ffi_str2ptr(payload78)
                              mbt_ffi_store32(
                                iter_base + 68,
                                payload78.length(),
                              )
                              mbt_ffi_store32(iter_base + 64, ptr79)
                              ()
                            }
                          }
                          let ptr80 = mbt_ffi_str2ptr(payload74.content)
                          mbt_ffi_store32(
                            iter_base + 76,
                            payload74.content.length(),
                          )
                          mbt_ffi_store32(iter_base + 72, ptr80)
                          let ptr81 = mbt_ffi_str2ptr(payload74.delimiter)
                          mbt_ffi_store32(
                            iter_base + 84,
                            payload74.delimiter.length(),
                          )
                          mbt_ffi_store32(iter_base + 80, ptr81)
                          ()
                        }
                        DataTable(payload82) => {
                          mbt_ffi_store8(iter_base + 44, 1)
                          mbt_ffi_store32(
                            iter_base + 48,
                            payload82.location.line,
                          )
                          match payload82.location.column {
                            None => {
                              mbt_ffi_store8(iter_base + 52, 0)
                              ()
                            }
                            Some(payload84) => {
                              mbt_ffi_store8(iter_base + 52, 1)
                              mbt_ffi_store32(iter_base + 56, payload84)
                              ()
                            }
                          }
                          let address93 = mbt_ffi_malloc(
                            payload82.rows.length() * 28,
                          )
                          for index94 = 0
                              index94 < payload82.rows.length()
                              index94 = index94 + 1 {
                            let iter_elem : @types.TableRow = payload82.rows[index94]
                            let iter_base = address93 + index94 * 28
                            mbt_ffi_store32(
                              iter_base + 0,
                              iter_elem.location.line,
                            )
                            match iter_elem.location.column {
                              None => {
                                mbt_ffi_store8(iter_base + 4, 0)
                                ()
                              }
                              Some(payload86) => {
                                mbt_ffi_store8(iter_base + 4, 1)
                                mbt_ffi_store32(iter_base + 8, payload86)
                                ()
                              }
                            }
                            let ptr87 = mbt_ffi_str2ptr(iter_elem.id)
                            mbt_ffi_store32(
                              iter_base + 16,
                              iter_elem.id.length(),
                            )
                            mbt_ffi_store32(iter_base + 12, ptr87)
                            let address91 = mbt_ffi_malloc(
                              iter_elem.cells.length() * 20,
                            )
                            for index92 = 0
                                index92 < iter_elem.cells.length()
                                index92 = index92 + 1 {
                              let iter_elem : @types.TableCell = iter_elem.cells[index92]
                              let iter_base = address91 + index92 * 20
                              mbt_ffi_store32(
                                iter_base + 0,
                                iter_elem.location.line,
                              )
                              match iter_elem.location.column {
                                None => {
                                  mbt_ffi_store8(iter_base + 4, 0)
                                  ()
                                }
                                Some(payload89) => {
                                  mbt_ffi_store8(iter_base + 4, 1)
                                  mbt_ffi_store32(iter_base + 8, payload89)
                                  ()
                                }
                              }
                              let ptr90 = mbt_ffi_str2ptr(iter_elem.value)
                              mbt_ffi_store32(
                                iter_base + 16,
                                iter_elem.value.length(),
                              )
                              mbt_ffi_store32(iter_base + 12, ptr90)
                            }
                            mbt_ffi_store32(
                              iter_base + 24,
                              iter_elem.cells.length(),
                            )
                            mbt_ffi_store32(iter_base + 20, address91)
                          }
                          mbt_ffi_store32(
                            iter_base + 64,
                            payload82.rows.length(),
                          )
                          mbt_ffi_store32(iter_base + 60, address93)
                          ()
                        }
                      }
                      ()
                    }
                  }
                }
                mbt_ffi_store32(iter_base + 64, payload54.steps.length())
                mbt_ffi_store32(iter_base + 60, address95)
                let address129 = mbt_ffi_malloc(
                  payload54.examples.length() * 92,
                )
                for index130 = 0
                    index130 < payload54.examples.length()
                    index130 = index130 + 1 {
                  let iter_elem : @types.Examples = payload54.examples[index130]
                  let iter_base = address129 + index130 * 92
                  mbt_ffi_store32(iter_base + 0, iter_elem.location.line)
                  match iter_elem.location.column {
                    None => {
                      mbt_ffi_store8(iter_base + 4, 0)
                      ()
                    }
                    Some(payload98) => {
                      mbt_ffi_store8(iter_base + 4, 1)
                      mbt_ffi_store32(iter_base + 8, payload98)
                      ()
                    }
                  }
                  let address103 = mbt_ffi_malloc(iter_elem.tags.length() * 28)
                  for index104 = 0
                      index104 < iter_elem.tags.length()
                      index104 = index104 + 1 {
                    let iter_elem : @types.Tag = iter_elem.tags[index104]
                    let iter_base = address103 + index104 * 28
                    mbt_ffi_store32(iter_base + 0, iter_elem.location.line)
                    match iter_elem.location.column {
                      None => {
                        mbt_ffi_store8(iter_base + 4, 0)
                        ()
                      }
                      Some(payload100) => {
                        mbt_ffi_store8(iter_base + 4, 1)
                        mbt_ffi_store32(iter_base + 8, payload100)
                        ()
                      }
                    }
                    let ptr101 = mbt_ffi_str2ptr(iter_elem.name)
                    mbt_ffi_store32(iter_base + 16, iter_elem.name.length())
                    mbt_ffi_store32(iter_base + 12, ptr101)
                    let ptr102 = mbt_ffi_str2ptr(iter_elem.id)
                    mbt_ffi_store32(iter_base + 24, iter_elem.id.length())
                    mbt_ffi_store32(iter_base + 20, ptr102)
                  }
                  mbt_ffi_store32(iter_base + 16, iter_elem.tags.length())
                  mbt_ffi_store32(iter_base + 12, address103)
                  let ptr105 = mbt_ffi_str2ptr(iter_elem.keyword)
                  mbt_ffi_store32(iter_base + 24, iter_elem.keyword.length())
                  mbt_ffi_store32(iter_base + 20, ptr105)
                  let ptr106 = mbt_ffi_str2ptr(iter_elem.name)
                  mbt_ffi_store32(iter_base + 32, iter_elem.name.length())
                  mbt_ffi_store32(iter_base + 28, ptr106)
                  let ptr107 = mbt_ffi_str2ptr(iter_elem.description)
                  mbt_ffi_store32(
                    iter_base + 40,
                    iter_elem.description.length(),
                  )
                  mbt_ffi_store32(iter_base + 36, ptr107)
                  let ptr108 = mbt_ffi_str2ptr(iter_elem.id)
                  mbt_ffi_store32(iter_base + 48, iter_elem.id.length())
                  mbt_ffi_store32(iter_base + 44, ptr108)
                  match iter_elem.table_header {
                    None => {
                      mbt_ffi_store8(iter_base + 52, 0)
                      ()
                    }
                    Some(payload110) => {
                      mbt_ffi_store8(iter_base + 52, 1)
                      mbt_ffi_store32(iter_base + 56, payload110.location.line)
                      match payload110.location.column {
                        None => {
                          mbt_ffi_store8(iter_base + 60, 0)
                          ()
                        }
                        Some(payload112) => {
                          mbt_ffi_store8(iter_base + 60, 1)
                          mbt_ffi_store32(iter_base + 64, payload112)
                          ()
                        }
                      }
                      let ptr113 = mbt_ffi_str2ptr(payload110.id)
                      mbt_ffi_store32(iter_base + 72, payload110.id.length())
                      mbt_ffi_store32(iter_base + 68, ptr113)
                      let address117 = mbt_ffi_malloc(
                        payload110.cells.length() * 20,
                      )
                      for index118 = 0
                          index118 < payload110.cells.length()
                          index118 = index118 + 1 {
                        let iter_elem : @types.TableCell = payload110.cells[index118]
                        let iter_base = address117 + index118 * 20
                        mbt_ffi_store32(iter_base + 0, iter_elem.location.line)
                        match iter_elem.location.column {
                          None => {
                            mbt_ffi_store8(iter_base + 4, 0)
                            ()
                          }
                          Some(payload115) => {
                            mbt_ffi_store8(iter_base + 4, 1)
                            mbt_ffi_store32(iter_base + 8, payload115)
                            ()
                          }
                        }
                        let ptr116 = mbt_ffi_str2ptr(iter_elem.value)
                        mbt_ffi_store32(
                          iter_base + 16,
                          iter_elem.value.length(),
                        )
                        mbt_ffi_store32(iter_base + 12, ptr116)
                      }
                      mbt_ffi_store32(iter_base + 80, payload110.cells.length())
                      mbt_ffi_store32(iter_base + 76, address117)
                      ()
                    }
                  }
                  let address127 = mbt_ffi_malloc(
                    iter_elem.table_body.length() * 28,
                  )
                  for index128 = 0
                      index128 < iter_elem.table_body.length()
                      index128 = index128 + 1 {
                    let iter_elem : @types.TableRow = iter_elem.table_body[index128]
                    let iter_base = address127 + index128 * 28
                    mbt_ffi_store32(iter_base + 0, iter_elem.location.line)
                    match iter_elem.location.column {
                      None => {
                        mbt_ffi_store8(iter_base + 4, 0)
                        ()
                      }
                      Some(payload120) => {
                        mbt_ffi_store8(iter_base + 4, 1)
                        mbt_ffi_store32(iter_base + 8, payload120)
                        ()
                      }
                    }
                    let ptr121 = mbt_ffi_str2ptr(iter_elem.id)
                    mbt_ffi_store32(iter_base + 16, iter_elem.id.length())
                    mbt_ffi_store32(iter_base + 12, ptr121)
                    let address125 = mbt_ffi_malloc(
                      iter_elem.cells.length() * 20,
                    )
                    for index126 = 0
                        index126 < iter_elem.cells.length()
                        index126 = index126 + 1 {
                      let iter_elem : @types.TableCell = iter_elem.cells[index126]
                      let iter_base = address125 + index126 * 20
                      mbt_ffi_store32(iter_base + 0, iter_elem.location.line)
                      match iter_elem.location.column {
                        None => {
                          mbt_ffi_store8(iter_base + 4, 0)
                          ()
                        }
                        Some(payload123) => {
                          mbt_ffi_store8(iter_base + 4, 1)
                          mbt_ffi_store32(iter_base + 8, payload123)
                          ()
                        }
                      }
                      let ptr124 = mbt_ffi_str2ptr(iter_elem.value)
                      mbt_ffi_store32(iter_base + 16, iter_elem.value.length())
                      mbt_ffi_store32(iter_base + 12, ptr124)
                    }
                    mbt_ffi_store32(iter_base + 24, iter_elem.cells.length())
                    mbt_ffi_store32(iter_base + 20, address125)
                  }
                  mbt_ffi_store32(iter_base + 88, iter_elem.table_body.length())
                  mbt_ffi_store32(iter_base + 84, address127)
                }
                mbt_ffi_store32(iter_base + 72, payload54.examples.length())
                mbt_ffi_store32(iter_base + 68, address129)
                ()
              }
              Rule(payload131) => {
                mbt_ffi_store8(iter_base + 0, 2)
                mbt_ffi_store32(iter_base + 4, payload131.location.line)
                match payload131.location.column {
                  None => {
                    mbt_ffi_store8(iter_base + 8, 0)
                    ()
                  }
                  Some(payload133) => {
                    mbt_ffi_store8(iter_base + 8, 1)
                    mbt_ffi_store32(iter_base + 12, payload133)
                    ()
                  }
                }
                let address138 = mbt_ffi_malloc(payload131.tags.length() * 28)
                for index139 = 0
                    index139 < payload131.tags.length()
                    index139 = index139 + 1 {
                  let iter_elem : @types.Tag = payload131.tags[index139]
                  let iter_base = address138 + index139 * 28
                  mbt_ffi_store32(iter_base + 0, iter_elem.location.line)
                  match iter_elem.location.column {
                    None => {
                      mbt_ffi_store8(iter_base + 4, 0)
                      ()
                    }
                    Some(payload135) => {
                      mbt_ffi_store8(iter_base + 4, 1)
                      mbt_ffi_store32(iter_base + 8, payload135)
                      ()
                    }
                  }
                  let ptr136 = mbt_ffi_str2ptr(iter_elem.name)
                  mbt_ffi_store32(iter_base + 16, iter_elem.name.length())
                  mbt_ffi_store32(iter_base + 12, ptr136)
                  let ptr137 = mbt_ffi_str2ptr(iter_elem.id)
                  mbt_ffi_store32(iter_base + 24, iter_elem.id.length())
                  mbt_ffi_store32(iter_base + 20, ptr137)
                }
                mbt_ffi_store32(iter_base + 20, payload131.tags.length())
                mbt_ffi_store32(iter_base + 16, address138)
                let ptr140 = mbt_ffi_str2ptr(payload131.keyword)
                mbt_ffi_store32(iter_base + 28, payload131.keyword.length())
                mbt_ffi_store32(iter_base + 24, ptr140)
                let ptr141 = mbt_ffi_str2ptr(payload131.name)
                mbt_ffi_store32(iter_base + 36, payload131.name.length())
                mbt_ffi_store32(iter_base + 32, ptr141)
                let ptr142 = mbt_ffi_str2ptr(payload131.description)
                mbt_ffi_store32(iter_base + 44, payload131.description.length())
                mbt_ffi_store32(iter_base + 40, ptr142)
                let ptr143 = mbt_ffi_str2ptr(payload131.id)
                mbt_ffi_store32(iter_base + 52, payload131.id.length())
                mbt_ffi_store32(iter_base + 48, ptr143)
                let address258 = mbt_ffi_malloc(
                  payload131.children.length() * 76,
                )
                for index259 = 0
                    index259 < payload131.children.length()
                    index259 = index259 + 1 {
                  let iter_elem : @types.RuleChild = payload131.children[index259]
                  let iter_base = address258 + index259 * 76
                  match iter_elem {
                    Background(payload144) => {
                      mbt_ffi_store8(iter_base + 0, 0)
                      mbt_ffi_store32(iter_base + 4, payload144.location.line)
                      match payload144.location.column {
                        None => {
                          mbt_ffi_store8(iter_base + 8, 0)
                          ()
                        }
                        Some(payload146) => {
                          mbt_ffi_store8(iter_base + 8, 1)
                          mbt_ffi_store32(iter_base + 12, payload146)
                          ()
                        }
                      }
                      let ptr147 = mbt_ffi_str2ptr(payload144.keyword)
                      mbt_ffi_store32(
                        iter_base + 20,
                        payload144.keyword.length(),
                      )
                      mbt_ffi_store32(iter_base + 16, ptr147)
                      let ptr148 = mbt_ffi_str2ptr(payload144.name)
                      mbt_ffi_store32(iter_base + 28, payload144.name.length())
                      mbt_ffi_store32(iter_base + 24, ptr148)
                      let ptr149 = mbt_ffi_str2ptr(payload144.description)
                      mbt_ffi_store32(
                        iter_base + 36,
                        payload144.description.length(),
                      )
                      mbt_ffi_store32(iter_base + 32, ptr149)
                      let ptr150 = mbt_ffi_str2ptr(payload144.id)
                      mbt_ffi_store32(iter_base + 44, payload144.id.length())
                      mbt_ffi_store32(iter_base + 40, ptr150)
                      let address179 = mbt_ffi_malloc(
                        payload144.steps.length() * 88,
                      )
                      for index180 = 0
                          index180 < payload144.steps.length()
                          index180 = index180 + 1 {
                        let iter_elem : @types.Step = payload144.steps[index180]
                        let iter_base = address179 + index180 * 88
                        mbt_ffi_store32(iter_base + 0, iter_elem.location.line)
                        match iter_elem.location.column {
                          None => {
                            mbt_ffi_store8(iter_base + 4, 0)
                            ()
                          }
                          Some(payload152) => {
                            mbt_ffi_store8(iter_base + 4, 1)
                            mbt_ffi_store32(iter_base + 8, payload152)
                            ()
                          }
                        }
                        let ptr153 = mbt_ffi_str2ptr(iter_elem.keyword)
                        mbt_ffi_store32(
                          iter_base + 16,
                          iter_elem.keyword.length(),
                        )
                        mbt_ffi_store32(iter_base + 12, ptr153)
                        mbt_ffi_store8(
                          iter_base + 20,
                          iter_elem.keyword_type.ordinal(),
                        )
                        let ptr154 = mbt_ffi_str2ptr(iter_elem.text)
                        mbt_ffi_store32(iter_base + 28, iter_elem.text.length())
                        mbt_ffi_store32(iter_base + 24, ptr154)
                        let ptr155 = mbt_ffi_str2ptr(iter_elem.id)
                        mbt_ffi_store32(iter_base + 36, iter_elem.id.length())
                        mbt_ffi_store32(iter_base + 32, ptr155)
                        match iter_elem.argument {
                          None => {
                            mbt_ffi_store8(iter_base + 40, 0)
                            ()
                          }
                          Some(payload157) => {
                            mbt_ffi_store8(iter_base + 40, 1)
                            match payload157 {
                              DocString(payload158) => {
                                mbt_ffi_store8(iter_base + 44, 0)
                                mbt_ffi_store32(
                                  iter_base + 48,
                                  payload158.location.line,
                                )
                                match payload158.location.column {
                                  None => {
                                    mbt_ffi_store8(iter_base + 52, 0)
                                    ()
                                  }
                                  Some(payload160) => {
                                    mbt_ffi_store8(iter_base + 52, 1)
                                    mbt_ffi_store32(iter_base + 56, payload160)
                                    ()
                                  }
                                }
                                match payload158.media_type {
                                  None => {
                                    mbt_ffi_store8(iter_base + 60, 0)
                                    ()
                                  }
                                  Some(payload162) => {
                                    mbt_ffi_store8(iter_base + 60, 1)
                                    let ptr163 = mbt_ffi_str2ptr(payload162)
                                    mbt_ffi_store32(
                                      iter_base + 68,
                                      payload162.length(),
                                    )
                                    mbt_ffi_store32(iter_base + 64, ptr163)
                                    ()
                                  }
                                }
                                let ptr164 = mbt_ffi_str2ptr(payload158.content)
                                mbt_ffi_store32(
                                  iter_base + 76,
                                  payload158.content.length(),
                                )
                                mbt_ffi_store32(iter_base + 72, ptr164)
                                let ptr165 = mbt_ffi_str2ptr(
                                  payload158.delimiter,
                                )
                                mbt_ffi_store32(
                                  iter_base + 84,
                                  payload158.delimiter.length(),
                                )
                                mbt_ffi_store32(iter_base + 80, ptr165)
                                ()
                              }
                              DataTable(payload166) => {
                                mbt_ffi_store8(iter_base + 44, 1)
                                mbt_ffi_store32(
                                  iter_base + 48,
                                  payload166.location.line,
                                )
                                match payload166.location.column {
                                  None => {
                                    mbt_ffi_store8(iter_base + 52, 0)
                                    ()
                                  }
                                  Some(payload168) => {
                                    mbt_ffi_store8(iter_base + 52, 1)
                                    mbt_ffi_store32(iter_base + 56, payload168)
                                    ()
                                  }
                                }
                                let address177 = mbt_ffi_malloc(
                                  payload166.rows.length() * 28,
                                )
                                for index178 = 0
                                    index178 < payload166.rows.length()
                                    index178 = index178 + 1 {
                                  let iter_elem : @types.TableRow = payload166.rows[index178]
                                  let iter_base = address177 + index178 * 28
                                  mbt_ffi_store32(
                                    iter_base + 0,
                                    iter_elem.location.line,
                                  )
                                  match iter_elem.location.column {
                                    None => {
                                      mbt_ffi_store8(iter_base + 4, 0)
                                      ()
                                    }
                                    Some(payload170) => {
                                      mbt_ffi_store8(iter_base + 4, 1)
                                      mbt_ffi_store32(iter_base + 8, payload170)
                                      ()
                                    }
                                  }
                                  let ptr171 = mbt_ffi_str2ptr(iter_elem.id)
                                  mbt_ffi_store32(
                                    iter_base + 16,
                                    iter_elem.id.length(),
                                  )
                                  mbt_ffi_store32(iter_base + 12, ptr171)
                                  let address175 = mbt_ffi_malloc(
                                    iter_elem.cells.length() * 20,
                                  )
                                  for index176 = 0
                                      index176 < iter_elem.cells.length()
                                      index176 = index176 + 1 {
                                    let iter_elem : @types.TableCell = iter_elem.cells[index176]
                                    let iter_base = address175 + index176 * 20
                                    mbt_ffi_store32(
                                      iter_base + 0,
                                      iter_elem.location.line,
                                    )
                                    match iter_elem.location.column {
                                      None => {
                                        mbt_ffi_store8(iter_base + 4, 0)
                                        ()
                                      }
                                      Some(payload173) => {
                                        mbt_ffi_store8(iter_base + 4, 1)
                                        mbt_ffi_store32(
                                          iter_base + 8,
                                          payload173,
                                        )
                                        ()
                                      }
                                    }
                                    let ptr174 = mbt_ffi_str2ptr(
                                      iter_elem.value,
                                    )
                                    mbt_ffi_store32(
                                      iter_base + 16,
                                      iter_elem.value.length(),
                                    )
                                    mbt_ffi_store32(iter_base + 12, ptr174)
                                  }
                                  mbt_ffi_store32(
                                    iter_base + 24,
                                    iter_elem.cells.length(),
                                  )
                                  mbt_ffi_store32(iter_base + 20, address175)
                                }
                                mbt_ffi_store32(
                                  iter_base + 64,
                                  payload166.rows.length(),
                                )
                                mbt_ffi_store32(iter_base + 60, address177)
                                ()
                              }
                            }
                            ()
                          }
                        }
                      }
                      mbt_ffi_store32(iter_base + 52, payload144.steps.length())
                      mbt_ffi_store32(iter_base + 48, address179)
                      ()
                    }
                    Scenario(payload181) => {
                      mbt_ffi_store8(iter_base + 0, 1)
                      mbt_ffi_store32(iter_base + 4, payload181.location.line)
                      match payload181.location.column {
                        None => {
                          mbt_ffi_store8(iter_base + 8, 0)
                          ()
                        }
                        Some(payload183) => {
                          mbt_ffi_store8(iter_base + 8, 1)
                          mbt_ffi_store32(iter_base + 12, payload183)
                          ()
                        }
                      }
                      let address188 = mbt_ffi_malloc(
                        payload181.tags.length() * 28,
                      )
                      for index189 = 0
                          index189 < payload181.tags.length()
                          index189 = index189 + 1 {
                        let iter_elem : @types.Tag = payload181.tags[index189]
                        let iter_base = address188 + index189 * 28
                        mbt_ffi_store32(iter_base + 0, iter_elem.location.line)
                        match iter_elem.location.column {
                          None => {
                            mbt_ffi_store8(iter_base + 4, 0)
                            ()
                          }
                          Some(payload185) => {
                            mbt_ffi_store8(iter_base + 4, 1)
                            mbt_ffi_store32(iter_base + 8, payload185)
                            ()
                          }
                        }
                        let ptr186 = mbt_ffi_str2ptr(iter_elem.name)
                        mbt_ffi_store32(iter_base + 16, iter_elem.name.length())
                        mbt_ffi_store32(iter_base + 12, ptr186)
                        let ptr187 = mbt_ffi_str2ptr(iter_elem.id)
                        mbt_ffi_store32(iter_base + 24, iter_elem.id.length())
                        mbt_ffi_store32(iter_base + 20, ptr187)
                      }
                      mbt_ffi_store32(iter_base + 20, payload181.tags.length())
                      mbt_ffi_store32(iter_base + 16, address188)
                      mbt_ffi_store8(iter_base + 24, payload181.kind.ordinal())
                      let ptr190 = mbt_ffi_str2ptr(payload181.keyword)
                      mbt_ffi_store32(
                        iter_base + 32,
                        payload181.keyword.length(),
                      )
                      mbt_ffi_store32(iter_base + 28, ptr190)
                      let ptr191 = mbt_ffi_str2ptr(payload181.name)
                      mbt_ffi_store32(iter_base + 40, payload181.name.length())
                      mbt_ffi_store32(iter_base + 36, ptr191)
                      let ptr192 = mbt_ffi_str2ptr(payload181.description)
                      mbt_ffi_store32(
                        iter_base + 48,
                        payload181.description.length(),
                      )
                      mbt_ffi_store32(iter_base + 44, ptr192)
                      let ptr193 = mbt_ffi_str2ptr(payload181.id)
                      mbt_ffi_store32(iter_base + 56, payload181.id.length())
                      mbt_ffi_store32(iter_base + 52, ptr193)
                      let address222 = mbt_ffi_malloc(
                        payload181.steps.length() * 88,
                      )
                      for index223 = 0
                          index223 < payload181.steps.length()
                          index223 = index223 + 1 {
                        let iter_elem : @types.Step = payload181.steps[index223]
                        let iter_base = address222 + index223 * 88
                        mbt_ffi_store32(iter_base + 0, iter_elem.location.line)
                        match iter_elem.location.column {
                          None => {
                            mbt_ffi_store8(iter_base + 4, 0)
                            ()
                          }
                          Some(payload195) => {
                            mbt_ffi_store8(iter_base + 4, 1)
                            mbt_ffi_store32(iter_base + 8, payload195)
                            ()
                          }
                        }
                        let ptr196 = mbt_ffi_str2ptr(iter_elem.keyword)
                        mbt_ffi_store32(
                          iter_base + 16,
                          iter_elem.keyword.length(),
                        )
                        mbt_ffi_store32(iter_base + 12, ptr196)
                        mbt_ffi_store8(
                          iter_base + 20,
                          iter_elem.keyword_type.ordinal(),
                        )
                        let ptr197 = mbt_ffi_str2ptr(iter_elem.text)
                        mbt_ffi_store32(iter_base + 28, iter_elem.text.length())
                        mbt_ffi_store32(iter_base + 24, ptr197)
                        let ptr198 = mbt_ffi_str2ptr(iter_elem.id)
                        mbt_ffi_store32(iter_base + 36, iter_elem.id.length())
                        mbt_ffi_store32(iter_base + 32, ptr198)
                        match iter_elem.argument {
                          None => {
                            mbt_ffi_store8(iter_base + 40, 0)
                            ()
                          }
                          Some(payload200) => {
                            mbt_ffi_store8(iter_base + 40, 1)
                            match payload200 {
                              DocString(payload201) => {
                                mbt_ffi_store8(iter_base + 44, 0)
                                mbt_ffi_store32(
                                  iter_base + 48,
                                  payload201.location.line,
                                )
                                match payload201.location.column {
                                  None => {
                                    mbt_ffi_store8(iter_base + 52, 0)
                                    ()
                                  }
                                  Some(payload203) => {
                                    mbt_ffi_store8(iter_base + 52, 1)
                                    mbt_ffi_store32(iter_base + 56, payload203)
                                    ()
                                  }
                                }
                                match payload201.media_type {
                                  None => {
                                    mbt_ffi_store8(iter_base + 60, 0)
                                    ()
                                  }
                                  Some(payload205) => {
                                    mbt_ffi_store8(iter_base + 60, 1)
                                    let ptr206 = mbt_ffi_str2ptr(payload205)
                                    mbt_ffi_store32(
                                      iter_base + 68,
                                      payload205.length(),
                                    )
                                    mbt_ffi_store32(iter_base + 64, ptr206)
                                    ()
                                  }
                                }
                                let ptr207 = mbt_ffi_str2ptr(payload201.content)
                                mbt_ffi_store32(
                                  iter_base + 76,
                                  payload201.content.length(),
                                )
                                mbt_ffi_store32(iter_base + 72, ptr207)
                                let ptr208 = mbt_ffi_str2ptr(
                                  payload201.delimiter,
                                )
                                mbt_ffi_store32(
                                  iter_base + 84,
                                  payload201.delimiter.length(),
                                )
                                mbt_ffi_store32(iter_base + 80, ptr208)
                                ()
                              }
                              DataTable(payload209) => {
                                mbt_ffi_store8(iter_base + 44, 1)
                                mbt_ffi_store32(
                                  iter_base + 48,
                                  payload209.location.line,
                                )
                                match payload209.location.column {
                                  None => {
                                    mbt_ffi_store8(iter_base + 52, 0)
                                    ()
                                  }
                                  Some(payload211) => {
                                    mbt_ffi_store8(iter_base + 52, 1)
                                    mbt_ffi_store32(iter_base + 56, payload211)
                                    ()
                                  }
                                }
                                let address220 = mbt_ffi_malloc(
                                  payload209.rows.length() * 28,
                                )
                                for index221 = 0
                                    index221 < payload209.rows.length()
                                    index221 = index221 + 1 {
                                  let iter_elem : @types.TableRow = payload209.rows[index221]
                                  let iter_base = address220 + index221 * 28
                                  mbt_ffi_store32(
                                    iter_base + 0,
                                    iter_elem.location.line,
                                  )
                                  match iter_elem.location.column {
                                    None => {
                                      mbt_ffi_store8(iter_base + 4, 0)
                                      ()
                                    }
                                    Some(payload213) => {
                                      mbt_ffi_store8(iter_base + 4, 1)
                                      mbt_ffi_store32(iter_base + 8, payload213)
                                      ()
                                    }
                                  }
                                  let ptr214 = mbt_ffi_str2ptr(iter_elem.id)
                                  mbt_ffi_store32(
                                    iter_base + 16,
                                    iter_elem.id.length(),
                                  )
                                  mbt_ffi_store32(iter_base + 12, ptr214)
                                  let address218 = mbt_ffi_malloc(
                                    iter_elem.cells.length() * 20,
                                  )
                                  for index219 = 0
                                      index219 < iter_elem.cells.length()
                                      index219 = index219 + 1 {
                                    let iter_elem : @types.TableCell = iter_elem.cells[index219]
                                    let iter_base = address218 + index219 * 20
                                    mbt_ffi_store32(
                                      iter_base + 0,
                                      iter_elem.location.line,
                                    )
                                    match iter_elem.location.column {
                                      None => {
                                        mbt_ffi_store8(iter_base + 4, 0)
                                        ()
                                      }
                                      Some(payload216) => {
                                        mbt_ffi_store8(iter_base + 4, 1)
                                        mbt_ffi_store32(
                                          iter_base + 8,
                                          payload216,
                                        )
                                        ()
                                      }
                                    }
                                    let ptr217 = mbt_ffi_str2ptr(
                                      iter_elem.value,
                                    )
                                    mbt_ffi_store32(
                                      iter_base + 16,
                                      iter_elem.value.length(),
                                    )
                                    mbt_ffi_store32(iter_base + 12, ptr217)
                                  }
                                  mbt_ffi_store32(
                                    iter_base + 24,
                                    iter_elem.cells.length(),
                                  )
                                  mbt_ffi_store32(iter_base + 20, address218)
                                }
                                mbt_ffi_store32(
                                  iter_base + 64,
                                  payload209.rows.length(),
                                )
                                mbt_ffi_store32(iter_base + 60, address220)
                                ()
                              }
                            }
                            ()
                          }
                        }
                      }
                      mbt_ffi_store32(iter_base + 64, payload181.steps.length())
                      mbt_ffi_store32(iter_base + 60, address222)
                      let address256 = mbt_ffi_malloc(
                        payload181.examples.length() * 92,
                      )
                      for index257 = 0
                          index257 < payload181.examples.length()
                          index257 = index257 + 1 {
                        let iter_elem : @types.Examples = payload181.examples[index257]
                        let iter_base = address256 + index257 * 92
                        mbt_ffi_store32(iter_base + 0, iter_elem.location.line)
                        match iter_elem.location.column {
                          None => {
                            mbt_ffi_store8(iter_base + 4, 0)
                            ()
                          }
                          Some(payload225) => {
                            mbt_ffi_store8(iter_base + 4, 1)
                            mbt_ffi_store32(iter_base + 8, payload225)
                            ()
                          }
                        }
                        let address230 = mbt_ffi_malloc(
                          iter_elem.tags.length() * 28,
                        )
                        for index231 = 0
                            index231 < iter_elem.tags.length()
                            index231 = index231 + 1 {
                          let iter_elem : @types.Tag = iter_elem.tags[index231]
                          let iter_base = address230 + index231 * 28
                          mbt_ffi_store32(
                            iter_base + 0,
                            iter_elem.location.line,
                          )
                          match iter_elem.location.column {
                            None => {
                              mbt_ffi_store8(iter_base + 4, 0)
                              ()
                            }
                            Some(payload227) => {
                              mbt_ffi_store8(iter_base + 4, 1)
                              mbt_ffi_store32(iter_base + 8, payload227)
                              ()
                            }
                          }
                          let ptr228 = mbt_ffi_str2ptr(iter_elem.name)
                          mbt_ffi_store32(
                            iter_base + 16,
                            iter_elem.name.length(),
                          )
                          mbt_ffi_store32(iter_base + 12, ptr228)
                          let ptr229 = mbt_ffi_str2ptr(iter_elem.id)
                          mbt_ffi_store32(iter_base + 24, iter_elem.id.length())
                          mbt_ffi_store32(iter_base + 20, ptr229)
                        }
                        mbt_ffi_store32(iter_base + 16, iter_elem.tags.length())
                        mbt_ffi_store32(iter_base + 12, address230)
                        let ptr232 = mbt_ffi_str2ptr(iter_elem.keyword)
                        mbt_ffi_store32(
                          iter_base + 24,
                          iter_elem.keyword.length(),
                        )
                        mbt_ffi_store32(iter_base + 20, ptr232)
                        let ptr233 = mbt_ffi_str2ptr(iter_elem.name)
                        mbt_ffi_store32(iter_base + 32, iter_elem.name.length())
                        mbt_ffi_store32(iter_base + 28, ptr233)
                        let ptr234 = mbt_ffi_str2ptr(iter_elem.description)
                        mbt_ffi_store32(
                          iter_base + 40,
                          iter_elem.description.length(),
                        )
                        mbt_ffi_store32(iter_base + 36, ptr234)
                        let ptr235 = mbt_ffi_str2ptr(iter_elem.id)
                        mbt_ffi_store32(iter_base + 48, iter_elem.id.length())
                        mbt_ffi_store32(iter_base + 44, ptr235)
                        match iter_elem.table_header {
                          None => {
                            mbt_ffi_store8(iter_base + 52, 0)
                            ()
                          }
                          Some(payload237) => {
                            mbt_ffi_store8(iter_base + 52, 1)
                            mbt_ffi_store32(
                              iter_base + 56,
                              payload237.location.line,
                            )
                            match payload237.location.column {
                              None => {
                                mbt_ffi_store8(iter_base + 60, 0)
                                ()
                              }
                              Some(payload239) => {
                                mbt_ffi_store8(iter_base + 60, 1)
                                mbt_ffi_store32(iter_base + 64, payload239)
                                ()
                              }
                            }
                            let ptr240 = mbt_ffi_str2ptr(payload237.id)
                            mbt_ffi_store32(
                              iter_base + 72,
                              payload237.id.length(),
                            )
                            mbt_ffi_store32(iter_base + 68, ptr240)
                            let address244 = mbt_ffi_malloc(
                              payload237.cells.length() * 20,
                            )
                            for index245 = 0
                                index245 < payload237.cells.length()
                                index245 = index245 + 1 {
                              let iter_elem : @types.TableCell = payload237.cells[index245]
                              let iter_base = address244 + index245 * 20
                              mbt_ffi_store32(
                                iter_base + 0,
                                iter_elem.location.line,
                              )
                              match iter_elem.location.column {
                                None => {
                                  mbt_ffi_store8(iter_base + 4, 0)
                                  ()
                                }
                                Some(payload242) => {
                                  mbt_ffi_store8(iter_base + 4, 1)
                                  mbt_ffi_store32(iter_base + 8, payload242)
                                  ()
                                }
                              }
                              let ptr243 = mbt_ffi_str2ptr(iter_elem.value)
                              mbt_ffi_store32(
                                iter_base + 16,
                                iter_elem.value.length(),
                              )
                              mbt_ffi_store32(iter_base + 12, ptr243)
                            }
                            mbt_ffi_store32(
                              iter_base + 80,
                              payload237.cells.length(),
                            )
                            mbt_ffi_store32(iter_base + 76, address244)
                            ()
                          }
                        }
                        let address254 = mbt_ffi_malloc(
                          iter_elem.table_body.length() * 28,
                        )
                        for index255 = 0
                            index255 < iter_elem.table_body.length()
                            index255 = index255 + 1 {
                          let iter_elem : @types.TableRow = iter_elem.table_body[index255]
                          let iter_base = address254 + index255 * 28
                          mbt_ffi_store32(
                            iter_base + 0,
                            iter_elem.location.line,
                          )
                          match iter_elem.location.column {
                            None => {
                              mbt_ffi_store8(iter_base + 4, 0)
                              ()
                            }
                            Some(payload247) => {
                              mbt_ffi_store8(iter_base + 4, 1)
                              mbt_ffi_store32(iter_base + 8, payload247)
                              ()
                            }
                          }
                          let ptr248 = mbt_ffi_str2ptr(iter_elem.id)
                          mbt_ffi_store32(iter_base + 16, iter_elem.id.length())
                          mbt_ffi_store32(iter_base + 12, ptr248)
                          let address252 = mbt_ffi_malloc(
                            iter_elem.cells.length() * 20,
                          )
                          for index253 = 0
                              index253 < iter_elem.cells.length()
                              index253 = index253 + 1 {
                            let iter_elem : @types.TableCell = iter_elem.cells[index253]
                            let iter_base = address252 + index253 * 20
                            mbt_ffi_store32(
                              iter_base + 0,
                              iter_elem.location.line,
                            )
                            match iter_elem.location.column {
                              None => {
                                mbt_ffi_store8(iter_base + 4, 0)
                                ()
                              }
                              Some(payload250) => {
                                mbt_ffi_store8(iter_base + 4, 1)
                                mbt_ffi_store32(iter_base + 8, payload250)
                                ()
                              }
                            }
                            let ptr251 = mbt_ffi_str2ptr(iter_elem.value)
                            mbt_ffi_store32(
                              iter_base + 16,
                              iter_elem.value.length(),
                            )
                            mbt_ffi_store32(iter_base + 12, ptr251)
                          }
                          mbt_ffi_store32(
                            iter_base + 24,
                            iter_elem.cells.length(),
                          )
                          mbt_ffi_store32(iter_base + 20, address252)
                        }
                        mbt_ffi_store32(
                          iter_base + 88,
                          iter_elem.table_body.length(),
                        )
                        mbt_ffi_store32(iter_base + 84, address254)
                      }
                      mbt_ffi_store32(
                        iter_base + 72,
                        payload181.examples.length(),
                      )
                      mbt_ffi_store32(iter_base + 68, address256)
                      ()
                    }
                  }
                }
                mbt_ffi_store32(iter_base + 60, payload131.children.length())
                mbt_ffi_store32(iter_base + 56, address258)
                ()
              }
            }
          }
          mbt_ffi_store32(return_area + 84, payload6.children.length())
          mbt_ffi_store32(return_area + 80, address260)
          ()
        }
      }
      let address265 = mbt_ffi_malloc(payload.comments.length() * 20)
      for index266 = 0
          index266 < payload.comments.length()
          index266 = index266 + 1 {
        let iter_elem : @types.Comment = payload.comments[index266]
        let iter_base = address265 + index266 * 20
        mbt_ffi_store32(iter_base + 0, iter_elem.location.line)
        match iter_elem.location.column {
          None => {
            mbt_ffi_store8(iter_base + 4, 0)
            ()
          }
          Some(payload263) => {
            mbt_ffi_store8(iter_base + 4, 1)
            mbt_ffi_store32(iter_base + 8, payload263)
            ()
          }
        }
        let ptr264 = mbt_ffi_str2ptr(iter_elem.text)
        mbt_ffi_store32(iter_base + 16, iter_elem.text.length())
        mbt_ffi_store32(iter_base + 12, ptr264)
      }
      mbt_ffi_store32(return_area + 92, payload.comments.length())
      mbt_ffi_store32(return_area + 88, address265)
      ()
    }
    Err(payload267) => {
      mbt_ffi_store8(return_area + 0, 1)
      let address271 = mbt_ffi_malloc(payload267.length() * 20)
      for index272 = 0; index272 < payload267.length(); index272 = index272 + 1 {
        let iter_elem : @types.ParseError = payload267[index272]
        let iter_base = address271 + index272 * 20
        let ptr268 = mbt_ffi_str2ptr(iter_elem.message)
        mbt_ffi_store32(iter_base + 4, iter_elem.message.length())
        mbt_ffi_store32(iter_base + 0, ptr268)
        mbt_ffi_store32(iter_base + 8, iter_elem.line)
        match iter_elem.column {
          None => {
            mbt_ffi_store8(iter_base + 12, 0)
            ()
          }
          Some(payload270) => {
            mbt_ffi_store8(iter_base + 12, 1)
            mbt_ffi_store32(iter_base + 16, payload270)
            ()
          }
        }
      }
      mbt_ffi_store32(return_area + 8, payload267.length())
      mbt_ffi_store32(return_area + 4, address271)
      ()
    }
  }
  return return_area
}

///|
pub fn wasmExportParsePostReturn(p0 : Int) -> Unit {
  match mbt_ffi_load8_u(p0 + 0) {
    0 => {
      match mbt_ffi_load8_u(p0 + 4) {
        0 => ()
        1 => mbt_ffi_free(mbt_ffi_load32(p0 + 8))
        _ => panic()
      }
      mbt_ffi_free(mbt_ffi_load32(p0 + 16))
      match mbt_ffi_load8_u(p0 + 24) {
        0 => ()
        1 => {
          for index = 0; index < mbt_ffi_load32(p0 + 44); index = index + 1 {
            let iter_base = mbt_ffi_load32(p0 + 40) + index * 28
            mbt_ffi_free(mbt_ffi_load32(iter_base + 12))
            mbt_ffi_free(mbt_ffi_load32(iter_base + 20))
          }
          mbt_ffi_free(mbt_ffi_load32(p0 + 40))
          mbt_ffi_free(mbt_ffi_load32(p0 + 48))
          mbt_ffi_free(mbt_ffi_load32(p0 + 56))
          mbt_ffi_free(mbt_ffi_load32(p0 + 64))
          mbt_ffi_free(mbt_ffi_load32(p0 + 72))
          for index26 = 0
              index26 < mbt_ffi_load32(p0 + 84)
              index26 = index26 + 1 {
            let iter_base = mbt_ffi_load32(p0 + 80) + index26 * 76
            match mbt_ffi_load8_u(iter_base + 0) {
              0 => {
                mbt_ffi_free(mbt_ffi_load32(iter_base + 16))
                mbt_ffi_free(mbt_ffi_load32(iter_base + 24))
                mbt_ffi_free(mbt_ffi_load32(iter_base + 32))
                mbt_ffi_free(mbt_ffi_load32(iter_base + 40))
                for index2 = 0
                    index2 < mbt_ffi_load32(iter_base + 52)
                    index2 = index2 + 1 {
                  let iter_base = mbt_ffi_load32(iter_base + 48) + index2 * 88
                  mbt_ffi_free(mbt_ffi_load32(iter_base + 12))
                  mbt_ffi_free(mbt_ffi_load32(iter_base + 24))
                  mbt_ffi_free(mbt_ffi_load32(iter_base + 32))
                  match mbt_ffi_load8_u(iter_base + 40) {
                    0 => ()
                    1 =>
                      match mbt_ffi_load8_u(iter_base + 44) {
                        0 => {
                          match mbt_ffi_load8_u(iter_base + 60) {
                            0 => ()
                            1 => mbt_ffi_free(mbt_ffi_load32(iter_base + 64))
                            _ => panic()
                          }
                          mbt_ffi_free(mbt_ffi_load32(iter_base + 72))
                          mbt_ffi_free(mbt_ffi_load32(iter_base + 80))
                        }
                        1 => {
                          for index1 = 0
                              index1 < mbt_ffi_load32(iter_base + 64)
                              index1 = index1 + 1 {
                            let iter_base = mbt_ffi_load32(iter_base + 60) +
                              index1 * 28
                            mbt_ffi_free(mbt_ffi_load32(iter_base + 12))
                            for index0 = 0
                                index0 < mbt_ffi_load32(iter_base + 24)
                                index0 = index0 + 1 {
                              let iter_base = mbt_ffi_load32(iter_base + 20) +
                                index0 * 20
                              mbt_ffi_free(mbt_ffi_load32(iter_base + 12))
                            }
                            mbt_ffi_free(mbt_ffi_load32(iter_base + 20))
                          }
                          mbt_ffi_free(mbt_ffi_load32(iter_base + 60))
                        }
                        _ => panic()
                      }
                    _ => panic()
                  }
                }
                mbt_ffi_free(mbt_ffi_load32(iter_base + 48))
              }
              1 => {
                for index3 = 0
                    index3 < mbt_ffi_load32(iter_base + 20)
                    index3 = index3 + 1 {
                  let iter_base = mbt_ffi_load32(iter_base + 16) + index3 * 28
                  mbt_ffi_free(mbt_ffi_load32(iter_base + 12))
                  mbt_ffi_free(mbt_ffi_load32(iter_base + 20))
                }
                mbt_ffi_free(mbt_ffi_load32(iter_base + 16))
                mbt_ffi_free(mbt_ffi_load32(iter_base + 28))
                mbt_ffi_free(mbt_ffi_load32(iter_base + 36))
                mbt_ffi_free(mbt_ffi_load32(iter_base + 44))
                mbt_ffi_free(mbt_ffi_load32(iter_base + 52))
                for index6 = 0
                    index6 < mbt_ffi_load32(iter_base + 64)
                    index6 = index6 + 1 {
                  let iter_base = mbt_ffi_load32(iter_base + 60) + index6 * 88
                  mbt_ffi_free(mbt_ffi_load32(iter_base + 12))
                  mbt_ffi_free(mbt_ffi_load32(iter_base + 24))
                  mbt_ffi_free(mbt_ffi_load32(iter_base + 32))
                  match mbt_ffi_load8_u(iter_base + 40) {
                    0 => ()
                    1 =>
                      match mbt_ffi_load8_u(iter_base + 44) {
                        0 => {
                          match mbt_ffi_load8_u(iter_base + 60) {
                            0 => ()
                            1 => mbt_ffi_free(mbt_ffi_load32(iter_base + 64))
                            _ => panic()
                          }
                          mbt_ffi_free(mbt_ffi_load32(iter_base + 72))
                          mbt_ffi_free(mbt_ffi_load32(iter_base + 80))
                        }
                        1 => {
                          for index5 = 0
                              index5 < mbt_ffi_load32(iter_base + 64)
                              index5 = index5 + 1 {
                            let iter_base = mbt_ffi_load32(iter_base + 60) +
                              index5 * 28
                            mbt_ffi_free(mbt_ffi_load32(iter_base + 12))
                            for index4 = 0
                                index4 < mbt_ffi_load32(iter_base + 24)
                                index4 = index4 + 1 {
                              let iter_base = mbt_ffi_load32(iter_base + 20) +
                                index4 * 20
                              mbt_ffi_free(mbt_ffi_load32(iter_base + 12))
                            }
                            mbt_ffi_free(mbt_ffi_load32(iter_base + 20))
                          }
                          mbt_ffi_free(mbt_ffi_load32(iter_base + 60))
                        }
                        _ => panic()
                      }
                    _ => panic()
                  }
                }
                mbt_ffi_free(mbt_ffi_load32(iter_base + 60))
                for index11 = 0
                    index11 < mbt_ffi_load32(iter_base + 72)
                    index11 = index11 + 1 {
                  let iter_base = mbt_ffi_load32(iter_base + 68) + index11 * 92
                  for index7 = 0
                      index7 < mbt_ffi_load32(iter_base + 16)
                      index7 = index7 + 1 {
                    let iter_base = mbt_ffi_load32(iter_base + 12) + index7 * 28
                    mbt_ffi_free(mbt_ffi_load32(iter_base + 12))
                    mbt_ffi_free(mbt_ffi_load32(iter_base + 20))
                  }
                  mbt_ffi_free(mbt_ffi_load32(iter_base + 12))
                  mbt_ffi_free(mbt_ffi_load32(iter_base + 20))
                  mbt_ffi_free(mbt_ffi_load32(iter_base + 28))
                  mbt_ffi_free(mbt_ffi_load32(iter_base + 36))
                  mbt_ffi_free(mbt_ffi_load32(iter_base + 44))
                  match mbt_ffi_load8_u(iter_base + 52) {
                    0 => ()
                    1 => {
                      mbt_ffi_free(mbt_ffi_load32(iter_base + 68))
                      for index8 = 0
                          index8 < mbt_ffi_load32(iter_base + 80)
                          index8 = index8 + 1 {
                        let iter_base = mbt_ffi_load32(iter_base + 76) +
                          index8 * 20
                        mbt_ffi_free(mbt_ffi_load32(iter_base + 12))
                      }
                      mbt_ffi_free(mbt_ffi_load32(iter_base + 76))
                    }
                    _ => panic()
                  }
                  for index10 = 0
                      index10 < mbt_ffi_load32(iter_base + 88)
                      index10 = index10 + 1 {
                    let iter_base = mbt_ffi_load32(iter_base + 84) +
                      index10 * 28
                    mbt_ffi_free(mbt_ffi_load32(iter_base + 12))
                    for index9 = 0
                        index9 < mbt_ffi_load32(iter_base + 24)
                        index9 = index9 + 1 {
                      let iter_base = mbt_ffi_load32(iter_base + 20) +
                        index9 * 20
                      mbt_ffi_free(mbt_ffi_load32(iter_base + 12))
                    }
                    mbt_ffi_free(mbt_ffi_load32(iter_base + 20))
                  }
                  mbt_ffi_free(mbt_ffi_load32(iter_base + 84))
                }
                mbt_ffi_free(mbt_ffi_load32(iter_base + 68))
              }
              2 => {
                for index12 = 0
                    index12 < mbt_ffi_load32(iter_base + 20)
                    index12 = index12 + 1 {
                  let iter_base = mbt_ffi_load32(iter_base + 16) + index12 * 28
                  mbt_ffi_free(mbt_ffi_load32(iter_base + 12))
                  mbt_ffi_free(mbt_ffi_load32(iter_base + 20))
                }
                mbt_ffi_free(mbt_ffi_load32(iter_base + 16))
                mbt_ffi_free(mbt_ffi_load32(iter_base + 24))
                mbt_ffi_free(mbt_ffi_load32(iter_base + 32))
                mbt_ffi_free(mbt_ffi_load32(iter_base + 40))
                mbt_ffi_free(mbt_ffi_load32(iter_base + 48))
                for index25 = 0
                    index25 < mbt_ffi_load32(iter_base + 60)
                    index25 = index25 + 1 {
                  let iter_base = mbt_ffi_load32(iter_base + 56) + index25 * 76
                  match mbt_ffi_load8_u(iter_base + 0) {
                    0 => {
                      mbt_ffi_free(mbt_ffi_load32(iter_base + 16))
                      mbt_ffi_free(mbt_ffi_load32(iter_base + 24))
                      mbt_ffi_free(mbt_ffi_load32(iter_base + 32))
                      mbt_ffi_free(mbt_ffi_load32(iter_base + 40))
                      for index15 = 0
                          index15 < mbt_ffi_load32(iter_base + 52)
                          index15 = index15 + 1 {
                        let iter_base = mbt_ffi_load32(iter_base + 48) +
                          index15 * 88
                        mbt_ffi_free(mbt_ffi_load32(iter_base + 12))
                        mbt_ffi_free(mbt_ffi_load32(iter_base + 24))
                        mbt_ffi_free(mbt_ffi_load32(iter_base + 32))
                        match mbt_ffi_load8_u(iter_base + 40) {
                          0 => ()
                          1 =>
                            match mbt_ffi_load8_u(iter_base + 44) {
                              0 => {
                                match mbt_ffi_load8_u(iter_base + 60) {
                                  0 => ()
                                  1 =>
                                    mbt_ffi_free(mbt_ffi_load32(iter_base + 64))
                                  _ => panic()
                                }
                                mbt_ffi_free(mbt_ffi_load32(iter_base + 72))
                                mbt_ffi_free(mbt_ffi_load32(iter_base + 80))
                              }
                              1 => {
                                for index14 = 0
                                    index14 < mbt_ffi_load32(iter_base + 64)
                                    index14 = index14 + 1 {
                                  let iter_base = mbt_ffi_load32(iter_base + 60) +
                                    index14 * 28
                                  mbt_ffi_free(mbt_ffi_load32(iter_base + 12))
                                  for index13 = 0
                                      index13 < mbt_ffi_load32(iter_base + 24)
                                      index13 = index13 + 1 {
                                    let iter_base = mbt_ffi_load32(
                                        iter_base + 20,
                                      ) +
                                      index13 * 20
                                    mbt_ffi_free(mbt_ffi_load32(iter_base + 12))
                                  }
                                  mbt_ffi_free(mbt_ffi_load32(iter_base + 20))
                                }
                                mbt_ffi_free(mbt_ffi_load32(iter_base + 60))
                              }
                              _ => panic()
                            }
                          _ => panic()
                        }
                      }
                      mbt_ffi_free(mbt_ffi_load32(iter_base + 48))
                    }
                    1 => {
                      for index16 = 0
                          index16 < mbt_ffi_load32(iter_base + 20)
                          index16 = index16 + 1 {
                        let iter_base = mbt_ffi_load32(iter_base + 16) +
                          index16 * 28
                        mbt_ffi_free(mbt_ffi_load32(iter_base + 12))
                        mbt_ffi_free(mbt_ffi_load32(iter_base + 20))
                      }
                      mbt_ffi_free(mbt_ffi_load32(iter_base + 16))
                      mbt_ffi_free(mbt_ffi_load32(iter_base + 28))
                      mbt_ffi_free(mbt_ffi_load32(iter_base + 36))
                      mbt_ffi_free(mbt_ffi_load32(iter_base + 44))
                      mbt_ffi_free(mbt_ffi_load32(iter_base + 52))
                      for index19 = 0
                          index19 < mbt_ffi_load32(iter_base + 64)
                          index19 = index19 + 1 {
                        let iter_base = mbt_ffi_load32(iter_base + 60) +
                          index19 * 88
                        mbt_ffi_free(mbt_ffi_load32(iter_base + 12))
                        mbt_ffi_free(mbt_ffi_load32(iter_base + 24))
                        mbt_ffi_free(mbt_ffi_load32(iter_base + 32))
                        match mbt_ffi_load8_u(iter_base + 40) {
                          0 => ()
                          1 =>
                            match mbt_ffi_load8_u(iter_base + 44) {
                              0 => {
                                match mbt_ffi_load8_u(iter_base + 60) {
                                  0 => ()
                                  1 =>
                                    mbt_ffi_free(mbt_ffi_load32(iter_base + 64))
                                  _ => panic()
                                }
                                mbt_ffi_free(mbt_ffi_load32(iter_base + 72))
                                mbt_ffi_free(mbt_ffi_load32(iter_base + 80))
                              }
                              1 => {
                                for index18 = 0
                                    index18 < mbt_ffi_load32(iter_base + 64)
                                    index18 = index18 + 1 {
                                  let iter_base = mbt_ffi_load32(iter_base + 60) +
                                    index18 * 28
                                  mbt_ffi_free(mbt_ffi_load32(iter_base + 12))
                                  for index17 = 0
                                      index17 < mbt_ffi_load32(iter_base + 24)
                                      index17 = index17 + 1 {
                                    let iter_base = mbt_ffi_load32(
                                        iter_base + 20,
                                      ) +
                                      index17 * 20
                                    mbt_ffi_free(mbt_ffi_load32(iter_base + 12))
                                  }
                                  mbt_ffi_free(mbt_ffi_load32(iter_base + 20))
                                }
                                mbt_ffi_free(mbt_ffi_load32(iter_base + 60))
                              }
                              _ => panic()
                            }
                          _ => panic()
                        }
                      }
                      mbt_ffi_free(mbt_ffi_load32(iter_base + 60))
                      for index24 = 0
                          index24 < mbt_ffi_load32(iter_base + 72)
                          index24 = index24 + 1 {
                        let iter_base = mbt_ffi_load32(iter_base + 68) +
                          index24 * 92
                        for index20 = 0
                            index20 < mbt_ffi_load32(iter_base + 16)
                            index20 = index20 + 1 {
                          let iter_base = mbt_ffi_load32(iter_base + 12) +
                            index20 * 28
                          mbt_ffi_free(mbt_ffi_load32(iter_base + 12))
                          mbt_ffi_free(mbt_ffi_load32(iter_base + 20))
                        }
                        mbt_ffi_free(mbt_ffi_load32(iter_base + 12))
                        mbt_ffi_free(mbt_ffi_load32(iter_base + 20))
                        mbt_ffi_free(mbt_ffi_load32(iter_base + 28))
                        mbt_ffi_free(mbt_ffi_load32(iter_base + 36))
                        mbt_ffi_free(mbt_ffi_load32(iter_base + 44))
                        match mbt_ffi_load8_u(iter_base + 52) {
                          0 => ()
                          1 => {
                            mbt_ffi_free(mbt_ffi_load32(iter_base + 68))
                            for index21 = 0
                                index21 < mbt_ffi_load32(iter_base + 80)
                                index21 = index21 + 1 {
                              let iter_base = mbt_ffi_load32(iter_base + 76) +
                                index21 * 20
                              mbt_ffi_free(mbt_ffi_load32(iter_base + 12))
                            }
                            mbt_ffi_free(mbt_ffi_load32(iter_base + 76))
                          }
                          _ => panic()
                        }
                        for index23 = 0
                            index23 < mbt_ffi_load32(iter_base + 88)
                            index23 = index23 + 1 {
                          let iter_base = mbt_ffi_load32(iter_base + 84) +
                            index23 * 28
                          mbt_ffi_free(mbt_ffi_load32(iter_base + 12))
                          for index22 = 0
                              index22 < mbt_ffi_load32(iter_base + 24)
                              index22 = index22 + 1 {
                            let iter_base = mbt_ffi_load32(iter_base + 20) +
                              index22 * 20
                            mbt_ffi_free(mbt_ffi_load32(iter_base + 12))
                          }
                          mbt_ffi_free(mbt_ffi_load32(iter_base + 20))
                        }
                        mbt_ffi_free(mbt_ffi_load32(iter_base + 84))
                      }
                      mbt_ffi_free(mbt_ffi_load32(iter_base + 68))
                    }
                    _ => panic()
                  }
                }
                mbt_ffi_free(mbt_ffi_load32(iter_base + 56))
              }
              _ => panic()
            }
          }
          mbt_ffi_free(mbt_ffi_load32(p0 + 80))
        }
        _ => panic()
      }
      for index27 = 0; index27 < mbt_ffi_load32(p0 + 92); index27 = index27 + 1 {
        let iter_base = mbt_ffi_load32(p0 + 88) + index27 * 20
        mbt_ffi_free(mbt_ffi_load32(iter_base + 12))
      }
      mbt_ffi_free(mbt_ffi_load32(p0 + 88))
    }
    1 => {
      for index28 = 0; index28 < mbt_ffi_load32(p0 + 8); index28 = index28 + 1 {
        let iter_base = mbt_ffi_load32(p0 + 4) + index28 * 20
        mbt_ffi_free(mbt_ffi_load32(iter_base + 0))
      }
      mbt_ffi_free(mbt_ffi_load32(p0 + 4))
    }
    _ => panic()
  }
}
