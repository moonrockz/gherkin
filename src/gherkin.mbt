// The moonrockz/gherkin library API is defined across:
//   types.mbt    - AST/DOM node types
//   error.mbt    - Parse error types
//   event.mbt    - Flat event types and GherkinEvent enum
//   parse.mbt    - Parse entry points
//   visitor.mbt  - GherkinVisitor trait
//   handler.mbt  - GherkinHandler trait (push model)
